package io.github.mmiimin.cosmicsurvival.Magic;

import org.bukkit.Location;
import org.bukkit.Particle;
import org.bukkit.entity.Entity;

public class MagicLaser {

/*
    public void castLaser(Entity caster, int distance, int angle){
        caster.getWorld().spawnParticle(Particle.DUST_COLOR_TRANSITION,);
        Location start = caster.eyeLocation
        Location to = start.clone().add(direction.clone().multiply(range))

    }
*/

}
